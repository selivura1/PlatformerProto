using System.Collections;
using System.Collections.Generic;
using System.Net.WebSockets;
using Selivura;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.TestTools;
using UnityEngine.UIElements;

public class ProjectileTest
{
    //[UnityTest]
    //public IEnumerator ProjectileMovementTest()
    //{
    //    yield return SceneManager.LoadSceneAsync(0);
    //    var projectileSpawner = GameObject.FindAnyObjectByType<ProjectilePool>();
    //    var database = GameObject.FindAnyObjectByType<Database>();
    //    var spawnedProjectile = projectileSpawner.GetProjectile(database.AllProjectiles[0]);
    //    spawnedProjectile.transform.right = Vector3.up;
    //    spawnedProjectile.Initialize();
    //    yield return new WaitForSeconds(spawnedProjectile.Lifetime);
    //}
}
